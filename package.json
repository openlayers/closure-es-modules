{
  "name": "closure-es-modules",
  "private": true,
  "version": "0.0.0",
  "description": "Testing the Closure Compiler with ES modules",
  "main": "index.js",
  "scripts": {
    "lint": "eslint build.js src",
    "pretest": "npm run lint",
    "test": "npm run build",
    "build": "node build.js",
    "jsdoc": "jsdoc --package package.json -r src/",
    "webpack": "webpack",
    "typecheck": "npm run build && npm run webpack"
  },
  "license": "ISC",
  "devDependencies": {
    "eslint": "^4.13.1",
    "eslint-config-openlayers": "^7.0.0",
    "google-closure-compiler": "^20171203.0.0",
    "jsdoc": "^3.5.5",
    "webpack": "^3.10.0"
  },
  "eslintConfig": {
    "extends": "openlayers",
    "env": {
      "es6": true
    }
  }
}
